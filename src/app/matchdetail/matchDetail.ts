export interface matchDetail{
        mapId: number
		matchCreation: number
		matchDuration: number
		matchId: number
		matchMode: string
		matchType: string;  
		matchVersion: string
		platformId: string
		queueType: string
		season: string
}